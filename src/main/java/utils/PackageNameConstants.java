package utils;


public final class PackageNameConstants {
    private PackageNameConstants() {
    }

    /**
     * Constant that represent the package name which holds all entities in the project.
     */
    public static final String ENTITIES_PACKAGE_NAME = "entity";
    /**
     * Constant that represent the package name which holds all services in the project.
     */
    public static final String SERVICES_PACKAGE_NAME = "service";
    /**
     * Constant that represent the package name which holds all repositories in the project.
     */
    public static final String REPOSITORIES_PACKAGE_NAME = "repository";
    /**
     * Constant that represent the package name which holds all controllers in the project.
     */
    public static final String CONTROLLER_PACKAGE_NAME = "controller";
    /**
     * Constant that represent the package name which holds all resolvers in the project.
     */
    public static final String RESOLVER_PACKAGE_NAME = "resolver";
}
